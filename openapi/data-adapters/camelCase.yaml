openapi: 3.0.3
info:
  title: Simple Test API
  version: 1.0.0
tags:
  - name: Test
    description: Test endpoints for data adapter behavior.

paths:
  /test:
    post:
      summary: Simple POST test
      description: Sends a simple request to validate camelCase payload and header handling.
      operationId: runCamelCaseAdapterTest
      tags:
        - Test
      parameters:
        - name: requestQuery
          in: query
          required: true
          description: Query value used by the test request.
          schema:
            type: string

        - name: requestHeader
          in: header
          required: true
          description: Header value required by the test request.
          schema:
            type: string

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - requestKey
              properties:
                requestKey:
                  type: string

      responses:
        "200":
          description: Success response
          headers:
            responseHeader:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                required:
                  - responseKey
                properties:
                  responseKey:
                    type: string
